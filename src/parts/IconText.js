import React from "react";
import Button from "elements/Button";

export default function IconText(props) {
  return (
    <Button href="/" className={props.className} type="link">
      POLARIS
    </Button>
  );
}
